The configuration module reads settings from environment variables and converts them to the types the application expects. It validates required values at startup, so a missing or malformed setting fails immediately instead of at first use. Keeping configuration outside the source means one build runs in every environment.

Unset required values raise ConfigError before the server binds a port. Optional values fall back to the defaults declared in the schema.
